# 再帰モード助数詞 (既存助数詞末尾に再帰的に付くもの)
#
# 例: 「個目」は 「個」を解決した後に 「メ」を末尾連結 → 一個目=イッコメ


[meta]
role = "counters"
description = "再帰モード助数詞 (個目 / 階目 等、 既存助数詞解決後に末尾連結)"

[counter."目"]
mode = "recursive"
suffix = "メ"
