# 人名用漢字 (法務省、 子の名に使用可、 常用と重複する 128 字を除外した残り 855 字)
#
# このファイルは tools/classify_unihan.py で水準別自動分類された後、
# tools/fill_missing_unihan.py で seed list の欠落分が KANJIDIC reading で補完された。
# 個別 entry の手動編集は OK (PR で reading 修正等)。

[meta]
schema_version = "2"
role = "unihan"
description = "人名用漢字 (法務省、 子の名に使用可、 常用と重複する 128 字を除外した残り 855 字)"

[entries]
# 丑 は core/kanji/overrides.toml の [[kanji]] block に移行
# 丞 は core/kanji/overrides.toml の [[kanji]] block に移行
# 乃 は core/kanji/overrides.toml の [[kanji]] block に移行
# 之 は core/kanji/overrides.toml の [[kanji]] block に移行
# 乎 は core/kanji/overrides.toml の [[kanji]] block に移行
# 也 は core/kanji/overrides.toml の [[kanji]] block に移行
# 云 は core/kanji/overrides.toml の [[kanji]] block に移行
# 亘 は core/kanji/overrides.toml の [[kanji]] block に移行
# 亙 は core/kanji/overrides.toml の [[kanji]] block に移行
# 些 は core/kanji/overrides.toml の [[kanji]] block に移行
# 亥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 亦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 亨 は core/kanji/overrides.toml の [[kanji]] block に移行
# 亮 は core/kanji/overrides.toml の [[kanji]] block に移行
# 仔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 伊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 伍 は core/kanji/overrides.toml の [[kanji]] block に移行
# 伶 は core/kanji/overrides.toml の [[kanji]] block に移行
# 伽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 佃 は core/kanji/overrides.toml の [[kanji]] block に移行
# 佑 は core/kanji/overrides.toml の [[kanji]] block に移行
# 侃 は core/kanji/overrides.toml の [[kanji]] block に移行
# 侑 は core/kanji/overrides.toml の [[kanji]] block に移行
# 俄 は core/kanji/overrides.toml の [[kanji]] block に移行
# 俐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 俠 は core/kanji/overrides.toml の [[kanji]] block に移行
# 俣 は core/kanji/overrides.toml の [[kanji]] block に移行
# 俱 は core/kanji/overrides.toml の [[kanji]] block に移行
# 倖 は core/kanji/overrides.toml の [[kanji]] block に移行
# 倦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 倭 は core/kanji/overrides.toml の [[kanji]] block に移行
# 偲 は core/kanji/overrides.toml の [[kanji]] block に移行
# 傭 は core/kanji/overrides.toml の [[kanji]] block に移行
# 儲 は core/kanji/overrides.toml の [[kanji]] block に移行
# 允 は core/kanji/overrides.toml の [[kanji]] block に移行
# 兎 は core/kanji/overrides.toml の [[kanji]] block に移行
# 兜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 其 は core/kanji/overrides.toml の [[kanji]] block に移行
# 冨 は core/kanji/overrides.toml の [[kanji]] block に移行
# 冴 は core/kanji/overrides.toml の [[kanji]] block に移行
# 凉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 凌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 凛 は core/kanji/overrides.toml の [[kanji]] block に移行
# 凜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 凧 は core/kanji/overrides.toml の [[kanji]] block に移行
# 凪 は core/kanji/overrides.toml の [[kanji]] block に移行
# 凰 は core/kanji/overrides.toml の [[kanji]] block に移行
# 凱 は core/kanji/overrides.toml の [[kanji]] block に移行
# 函 は core/kanji/overrides.toml の [[kanji]] block に移行
# 劉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 劫 は core/kanji/overrides.toml の [[kanji]] block に移行
# 勁 は core/kanji/overrides.toml の [[kanji]] block に移行
# 勿 は core/kanji/overrides.toml の [[kanji]] block に移行
# 匡 は core/kanji/overrides.toml の [[kanji]] block に移行
# 卜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 卯 は core/kanji/overrides.toml の [[kanji]] block に移行
# 卿 は core/kanji/overrides.toml の [[kanji]] block に移行
# 厨 は core/kanji/overrides.toml の [[kanji]] block に移行
# 厩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 叉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 叡 は core/kanji/overrides.toml の [[kanji]] block に移行
# 叢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 只 は core/kanji/overrides.toml の [[kanji]] block に移行
# 叶 は core/kanji/overrides.toml の [[kanji]] block に移行
# 吞 は core/kanji/overrides.toml の [[kanji]] block に移行
# 吻 は core/kanji/overrides.toml の [[kanji]] block に移行
# 吾 は core/kanji/overrides.toml の [[kanji]] block に移行
# 哉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 哨 は core/kanji/overrides.toml の [[kanji]] block に移行
# 哩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 啄 は core/kanji/overrides.toml の [[kanji]] block に移行
# 喋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 喧 は core/kanji/overrides.toml の [[kanji]] block に移行
# 喬 は core/kanji/overrides.toml の [[kanji]] block に移行
# 喰 は core/kanji/overrides.toml の [[kanji]] block に移行
# 嘉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 嘗 は core/kanji/overrides.toml の [[kanji]] block に移行
# 嘩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 噂 は core/kanji/overrides.toml の [[kanji]] block に移行
# 噌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 圃 は core/kanji/overrides.toml の [[kanji]] block に移行
# 圭 は core/kanji/overrides.toml の [[kanji]] block に移行
# 坐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 坦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 埜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 埴 は core/kanji/overrides.toml の [[kanji]] block に移行
# 堰 は core/kanji/overrides.toml の [[kanji]] block に移行
# 堵 は core/kanji/overrides.toml の [[kanji]] block に移行
# 堺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 塙 は core/kanji/overrides.toml の [[kanji]] block に移行
# 壕 は core/kanji/overrides.toml の [[kanji]] block に移行
# 壬 は core/kanji/overrides.toml の [[kanji]] block に移行
# 夷 は core/kanji/overrides.toml の [[kanji]] block に移行
# 奄 は core/kanji/overrides.toml の [[kanji]] block に移行
# 奎 は core/kanji/overrides.toml の [[kanji]] block に移行
# 套 は core/kanji/overrides.toml の [[kanji]] block に移行
# 姥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 姪 は core/kanji/overrides.toml の [[kanji]] block に移行
# 娃 は core/kanji/overrides.toml の [[kanji]] block に移行
# 娩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 嬉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 孜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 孟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 宋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 宏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 宕 は core/kanji/overrides.toml の [[kanji]] block に移行
# 宥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 寅 は core/kanji/overrides.toml の [[kanji]] block に移行
# 寓 は core/kanji/overrides.toml の [[kanji]] block に移行
# 寵 は core/kanji/overrides.toml の [[kanji]] block に移行
# 尖 は core/kanji/overrides.toml の [[kanji]] block に移行
# 尤 は core/kanji/overrides.toml の [[kanji]] block に移行
# 尭 は core/kanji/overrides.toml の [[kanji]] block に移行
# 屑 は core/kanji/overrides.toml の [[kanji]] block に移行
# 峨 は core/kanji/overrides.toml の [[kanji]] block に移行
# 峯 は core/kanji/overrides.toml の [[kanji]] block に移行
# 峻 は core/kanji/overrides.toml の [[kanji]] block に移行
# 崚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 嵩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 嵯 は core/kanji/overrides.toml の [[kanji]] block に移行
# 嶺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 巌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 已 は core/kanji/overrides.toml の [[kanji]] block に移行
# 巳 は core/kanji/overrides.toml の [[kanji]] block に移行
# 巴 は core/kanji/overrides.toml の [[kanji]] block に移行
# 巷 は core/kanji/overrides.toml の [[kanji]] block に移行
# 巽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 帖 は core/kanji/overrides.toml の [[kanji]] block に移行
# 幌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 幡 は core/kanji/overrides.toml の [[kanji]] block に移行
# 庄 は core/kanji/overrides.toml の [[kanji]] block に移行
# 庇 は core/kanji/overrides.toml の [[kanji]] block に移行
# 庚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 庵 は core/kanji/overrides.toml の [[kanji]] block に移行
# 廟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 廻 は core/kanji/overrides.toml の [[kanji]] block に移行
# 弘 は core/kanji/overrides.toml の [[kanji]] block に移行
# 弛 は core/kanji/overrides.toml の [[kanji]] block に移行
# 彗 は core/kanji/overrides.toml の [[kanji]] block に移行
# 彦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 彪 は core/kanji/overrides.toml の [[kanji]] block に移行
# 彬 は core/kanji/overrides.toml の [[kanji]] block に移行
# 徠 は core/kanji/overrides.toml の [[kanji]] block に移行
# 徽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 忽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 怜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 恕 は core/kanji/overrides.toml の [[kanji]] block に移行
# 恢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 恰 は core/kanji/overrides.toml の [[kanji]] block に移行
# 悉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 悌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 惇 は core/kanji/overrides.toml の [[kanji]] block に移行
# 惚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 惟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 惣 は core/kanji/overrides.toml の [[kanji]] block に移行
# 惹 は core/kanji/overrides.toml の [[kanji]] block に移行
# 惺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 慧 は core/kanji/overrides.toml の [[kanji]] block に移行
# 憐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 戊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 或 は core/kanji/overrides.toml の [[kanji]] block に移行
# 戟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 托 は core/kanji/overrides.toml の [[kanji]] block に移行
# 按 は core/kanji/overrides.toml の [[kanji]] block に移行
# 挺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 挽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 捧 は core/kanji/overrides.toml の [[kanji]] block に移行
# 捲 は core/kanji/overrides.toml の [[kanji]] block に移行
# 捷 は core/kanji/overrides.toml の [[kanji]] block に移行
# 捺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 掠 は core/kanji/overrides.toml の [[kanji]] block に移行
# 掬 は core/kanji/overrides.toml の [[kanji]] block に移行
# 揃 は core/kanji/overrides.toml の [[kanji]] block に移行
# 摑 は core/kanji/overrides.toml の [[kanji]] block に移行
# 摺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 撒 は core/kanji/overrides.toml の [[kanji]] block に移行
# 撞 は core/kanji/overrides.toml の [[kanji]] block に移行
# 撫 は core/kanji/overrides.toml の [[kanji]] block に移行
# 播 は core/kanji/overrides.toml の [[kanji]] block に移行
# 撰 は core/kanji/overrides.toml の [[kanji]] block に移行
# 擢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 敦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 斐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 斡 は core/kanji/overrides.toml の [[kanji]] block に移行
# 斧 は core/kanji/overrides.toml の [[kanji]] block に移行
# 斯 は core/kanji/overrides.toml の [[kanji]] block に移行
# 於 は core/kanji/overrides.toml の [[kanji]] block に移行
# 旭 は core/kanji/overrides.toml の [[kanji]] block に移行
# 昂 は core/kanji/overrides.toml の [[kanji]] block に移行
# 昊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 昌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 昏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 昴 は core/kanji/overrides.toml の [[kanji]] block に移行
# 晃 は core/kanji/overrides.toml の [[kanji]] block に移行
# 晄 は core/kanji/overrides.toml の [[kanji]] block に移行
# 晋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 晏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 晒 は core/kanji/overrides.toml の [[kanji]] block に移行
# 晚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 晟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 晦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 晨 は core/kanji/overrides.toml の [[kanji]] block に移行
# 智 は core/kanji/overrides.toml の [[kanji]] block に移行
# 暉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 暢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 曙 は core/kanji/overrides.toml の [[kanji]] block に移行
# 曝 は core/kanji/overrides.toml の [[kanji]] block に移行
# 曳 は core/kanji/overrides.toml の [[kanji]] block に移行
# 朋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 朔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 李 は core/kanji/overrides.toml の [[kanji]] block に移行
# 杏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 杖 は core/kanji/overrides.toml の [[kanji]] block に移行
# 杜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 杭 は core/kanji/overrides.toml の [[kanji]] block に移行
# 杵 は core/kanji/overrides.toml の [[kanji]] block に移行
# 杷 は core/kanji/overrides.toml の [[kanji]] block に移行
# 枇 は core/kanji/overrides.toml の [[kanji]] block に移行
# 柊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 柏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 柑 は core/kanji/overrides.toml の [[kanji]] block に移行
# 柘 は core/kanji/overrides.toml の [[kanji]] block に移行
# 柚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 柴 は core/kanji/overrides.toml の [[kanji]] block に移行
# 柾 は core/kanji/overrides.toml の [[kanji]] block に移行
# 栖 は core/kanji/overrides.toml の [[kanji]] block に移行
# 栗 は core/kanji/overrides.toml の [[kanji]] block に移行
# 栞 は core/kanji/overrides.toml の [[kanji]] block に移行
# 桂 は core/kanji/overrides.toml の [[kanji]] block に移行
# 桐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 桔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 桧 は core/kanji/overrides.toml の [[kanji]] block に移行
# 桶 は core/kanji/overrides.toml の [[kanji]] block に移行
# 梁 は core/kanji/overrides.toml の [[kanji]] block に移行
# 梓 は core/kanji/overrides.toml の [[kanji]] block に移行
# 梛 は core/kanji/overrides.toml の [[kanji]] block に移行
# 梢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 梧 は core/kanji/overrides.toml の [[kanji]] block に移行
# 梯 は core/kanji/overrides.toml の [[kanji]] block に移行
# 梶 は core/kanji/overrides.toml の [[kanji]] block に移行
# 棲 は core/kanji/overrides.toml の [[kanji]] block に移行
# 椀 は core/kanji/overrides.toml の [[kanji]] block に移行
# 椋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 椛 は core/kanji/overrides.toml の [[kanji]] block に移行
# 椰 は core/kanji/overrides.toml の [[kanji]] block に移行
# 椿 は core/kanji/overrides.toml の [[kanji]] block に移行
# 楊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 楓 は core/kanji/overrides.toml の [[kanji]] block に移行
# 楕 は core/kanji/overrides.toml の [[kanji]] block に移行
# 楚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 楠 は core/kanji/overrides.toml の [[kanji]] block に移行
# 楢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 楯 は core/kanji/overrides.toml の [[kanji]] block に移行
# 榊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 榎 は core/kanji/overrides.toml の [[kanji]] block に移行
# 榛 は core/kanji/overrides.toml の [[kanji]] block に移行
# 槌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 槍 は core/kanji/overrides.toml の [[kanji]] block に移行
# 槙 は core/kanji/overrides.toml の [[kanji]] block に移行
# 槻 は core/kanji/overrides.toml の [[kanji]] block に移行
# 樋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 樟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 樫 は core/kanji/overrides.toml の [[kanji]] block に移行
# 樺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 樽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 橘 は core/kanji/overrides.toml の [[kanji]] block に移行
# 橙 は core/kanji/overrides.toml の [[kanji]] block に移行
# 檀 は core/kanji/overrides.toml の [[kanji]] block に移行
# 檎 は core/kanji/overrides.toml の [[kanji]] block に移行
# 櫂 は core/kanji/overrides.toml の [[kanji]] block に移行
# 櫓 は core/kanji/overrides.toml の [[kanji]] block に移行
# 櫛 は core/kanji/overrides.toml の [[kanji]] block に移行
# 欣 は core/kanji/overrides.toml の [[kanji]] block に移行
# 欽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 歎 は core/kanji/overrides.toml の [[kanji]] block に移行
# 此 は core/kanji/overrides.toml の [[kanji]] block に移行
# 殆 は core/kanji/overrides.toml の [[kanji]] block に移行
# 毅 は core/kanji/overrides.toml の [[kanji]] block に移行
# 毘 は core/kanji/overrides.toml の [[kanji]] block に移行
# 毬 は core/kanji/overrides.toml の [[kanji]] block に移行
# 汀 は core/kanji/overrides.toml の [[kanji]] block に移行
# 汐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 汝 は core/kanji/overrides.toml の [[kanji]] block に移行
# 汲 は core/kanji/overrides.toml の [[kanji]] block に移行
# 沌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 沓 は core/kanji/overrides.toml の [[kanji]] block に移行
# 沫 は core/kanji/overrides.toml の [[kanji]] block に移行
# 洛 は core/kanji/overrides.toml の [[kanji]] block に移行
# 洲 は core/kanji/overrides.toml の [[kanji]] block に移行
# 洵 は core/kanji/overrides.toml の [[kanji]] block に移行
# 洸 は core/kanji/overrides.toml の [[kanji]] block に移行
# 浩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 浬 は core/kanji/overrides.toml の [[kanji]] block に移行
# 淀 は core/kanji/overrides.toml の [[kanji]] block に移行
# 淋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 淳 は core/kanji/overrides.toml の [[kanji]] block に移行
# 淵 は core/kanji/overrides.toml の [[kanji]] block に移行
# 渚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 渥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 湊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 湘 は core/kanji/overrides.toml の [[kanji]] block に移行
# 湛 は core/kanji/overrides.toml の [[kanji]] block に移行
# 溜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 溢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 滉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 漕 は core/kanji/overrides.toml の [[kanji]] block に移行
# 漣 は core/kanji/overrides.toml の [[kanji]] block に移行
# 漱 は core/kanji/overrides.toml の [[kanji]] block に移行
# 澪 は core/kanji/overrides.toml の [[kanji]] block に移行
# 濡 は core/kanji/overrides.toml の [[kanji]] block に移行
# 瀕 は core/kanji/overrides.toml の [[kanji]] block に移行
# 灘 は core/kanji/overrides.toml の [[kanji]] block に移行
# 灸 は core/kanji/overrides.toml の [[kanji]] block に移行
# 灼 は core/kanji/overrides.toml の [[kanji]] block に移行
# 烏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 焚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 焰 は core/kanji/overrides.toml の [[kanji]] block に移行
# 煉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 煌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 煤 は core/kanji/overrides.toml の [[kanji]] block に移行
# 熙 は core/kanji/overrides.toml の [[kanji]] block に移行
# 燎 は core/kanji/overrides.toml の [[kanji]] block に移行
# 燕 は core/kanji/overrides.toml の [[kanji]] block に移行
# 燦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 燭 は core/kanji/overrides.toml の [[kanji]] block に移行
# 燿 は core/kanji/overrides.toml の [[kanji]] block に移行
# 爾 は core/kanji/overrides.toml の [[kanji]] block に移行
# 牒 は core/kanji/overrides.toml の [[kanji]] block に移行
# 牟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 牡 は core/kanji/overrides.toml の [[kanji]] block に移行
# 牽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 犀 は core/kanji/overrides.toml の [[kanji]] block に移行
# 狼 は core/kanji/overrides.toml の [[kanji]] block に移行
# 猪 は core/kanji/overrides.toml の [[kanji]] block に移行
# 獅 は core/kanji/overrides.toml の [[kanji]] block に移行
# 玖 は core/kanji/overrides.toml の [[kanji]] block に移行
# 玲 は core/kanji/overrides.toml の [[kanji]] block に移行
# 珀 は core/kanji/overrides.toml の [[kanji]] block に移行
# 珂 は core/kanji/overrides.toml の [[kanji]] block に移行
# 珈 は core/kanji/overrides.toml の [[kanji]] block に移行
# 珊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 琉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 琢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 琥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 琳 は core/kanji/overrides.toml の [[kanji]] block に移行
# 琵 は core/kanji/overrides.toml の [[kanji]] block に移行
# 琶 は core/kanji/overrides.toml の [[kanji]] block に移行
# 瑚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 瑛 は core/kanji/overrides.toml の [[kanji]] block に移行
# 瑞 は core/kanji/overrides.toml の [[kanji]] block に移行
# 瑳 は core/kanji/overrides.toml の [[kanji]] block に移行
# 瑶 は core/kanji/overrides.toml の [[kanji]] block に移行
# 瓜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 瓢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 甥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 甫 は core/kanji/overrides.toml の [[kanji]] block に移行
# 畠 は core/kanji/overrides.toml の [[kanji]] block に移行
# 畢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 疋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 疏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 皐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 皓 は core/kanji/overrides.toml の [[kanji]] block に移行
# 盃 は core/kanji/overrides.toml の [[kanji]] block に移行
# 眸 は core/kanji/overrides.toml の [[kanji]] block に移行
# 瞥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 矩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 砥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 砦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 砧 は core/kanji/overrides.toml の [[kanji]] block に移行
# 硯 は core/kanji/overrides.toml の [[kanji]] block に移行
# 碓 は core/kanji/overrides.toml の [[kanji]] block に移行
# 碗 は core/kanji/overrides.toml の [[kanji]] block に移行
# 碧 は core/kanji/overrides.toml の [[kanji]] block に移行
# 碩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 磐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 磯 は core/kanji/overrides.toml の [[kanji]] block に移行
# 祁 は core/kanji/overrides.toml の [[kanji]] block に移行
# 祇 は core/kanji/overrides.toml の [[kanji]] block に移行
# 祐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 祢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 禄 は core/kanji/overrides.toml の [[kanji]] block に移行
# 禎 は core/kanji/overrides.toml の [[kanji]] block に移行
# 禰 は core/kanji/overrides.toml の [[kanji]] block に移行
# 禱 は core/kanji/overrides.toml の [[kanji]] block に移行
# 禽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 禾 は部首字 reading (例: ぎょうにんべん) を持つため削除、 文章中に出ても IPADIC fallback で読む
# 秤 は core/kanji/overrides.toml の [[kanji]] block に移行
# 秦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 稀 は core/kanji/overrides.toml の [[kanji]] block に移行
# 稔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 稜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 稟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 穣 は core/kanji/overrides.toml の [[kanji]] block に移行
# 穿 は core/kanji/overrides.toml の [[kanji]] block に移行
# 窄 は core/kanji/overrides.toml の [[kanji]] block に移行
# 窪 は core/kanji/overrides.toml の [[kanji]] block に移行
# 窺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 竣 は core/kanji/overrides.toml の [[kanji]] block に移行
# 竪 は core/kanji/overrides.toml の [[kanji]] block に移行
# 竺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 竿 は core/kanji/overrides.toml の [[kanji]] block に移行
# 笈 は core/kanji/overrides.toml の [[kanji]] block に移行
# 笙 は core/kanji/overrides.toml の [[kanji]] block に移行
# 笠 は core/kanji/overrides.toml の [[kanji]] block に移行
# 笹 は core/kanji/overrides.toml の [[kanji]] block に移行
# 筈 は core/kanji/overrides.toml の [[kanji]] block に移行
# 筑 は core/kanji/overrides.toml の [[kanji]] block に移行
# 箔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 箕 は core/kanji/overrides.toml の [[kanji]] block に移行
# 篇 は core/kanji/overrides.toml の [[kanji]] block に移行
# 篠 は core/kanji/overrides.toml の [[kanji]] block に移行
# 簞 は core/kanji/overrides.toml の [[kanji]] block に移行
# 簾 は core/kanji/overrides.toml の [[kanji]] block に移行
# 籾 は core/kanji/overrides.toml の [[kanji]] block に移行
# 粟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 粥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 糊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 紐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 紗 は core/kanji/overrides.toml の [[kanji]] block に移行
# 紘 は core/kanji/overrides.toml の [[kanji]] block に移行
# 紬 は core/kanji/overrides.toml の [[kanji]] block に移行
# 絃 は core/kanji/overrides.toml の [[kanji]] block に移行
# 絆 は core/kanji/overrides.toml の [[kanji]] block に移行
# 絢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 綜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 綴 は core/kanji/overrides.toml の [[kanji]] block に移行
# 綸 は core/kanji/overrides.toml の [[kanji]] block に移行
# 綺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 綾 は core/kanji/overrides.toml の [[kanji]] block に移行
# 緋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 縞 は core/kanji/overrides.toml の [[kanji]] block に移行
# 繡 は core/kanji/overrides.toml の [[kanji]] block に移行
# 繫 は core/kanji/overrides.toml の [[kanji]] block に移行
# 纂 は core/kanji/overrides.toml の [[kanji]] block に移行
# 纏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 羚 は core/kanji/overrides.toml の [[kanji]] block に移行
# 翔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 翠 は core/kanji/overrides.toml の [[kanji]] block に移行
# 耀 は core/kanji/overrides.toml の [[kanji]] block に移行
# 而 は core/kanji/overrides.toml の [[kanji]] block に移行
# 耶 は core/kanji/overrides.toml の [[kanji]] block に移行
# 耽 は core/kanji/overrides.toml の [[kanji]] block に移行
# 聡 は core/kanji/overrides.toml の [[kanji]] block に移行
# 肇 は core/kanji/overrides.toml の [[kanji]] block に移行
# 肋 は core/kanji/overrides.toml の [[kanji]] block に移行
# 肴 は core/kanji/overrides.toml の [[kanji]] block に移行
# 胡 は core/kanji/overrides.toml の [[kanji]] block に移行
# 胤 は core/kanji/overrides.toml の [[kanji]] block に移行
# 脩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 腔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 膏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 臥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 舜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 舵 は core/kanji/overrides.toml の [[kanji]] block に移行
# 芙 は core/kanji/overrides.toml の [[kanji]] block に移行
# 芥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 芦 は core/kanji/overrides.toml の [[kanji]] block に移行
# 芭 は core/kanji/overrides.toml の [[kanji]] block に移行
# 芹 は core/kanji/overrides.toml の [[kanji]] block に移行
# 苑 は core/kanji/overrides.toml の [[kanji]] block に移行
# 苔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 苺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 茄 は core/kanji/overrides.toml の [[kanji]] block に移行
# 茅 は core/kanji/overrides.toml の [[kanji]] block に移行
# 茉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 茜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 茸 は core/kanji/overrides.toml の [[kanji]] block に移行
# 荻 は core/kanji/overrides.toml の [[kanji]] block に移行
# 莉 は core/kanji/overrides.toml の [[kanji]] block に移行
# 莞 は core/kanji/overrides.toml の [[kanji]] block に移行
# 莫 は core/kanji/overrides.toml の [[kanji]] block に移行
# 菅 は core/kanji/overrides.toml の [[kanji]] block に移行
# 菖 は core/kanji/overrides.toml の [[kanji]] block に移行
# 菩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 菫 は core/kanji/overrides.toml の [[kanji]] block に移行
# 菱 は core/kanji/overrides.toml の [[kanji]] block に移行
"萄" = "ドウ"
"萊" = "ライ"
# 萌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 萠 は core/kanji/overrides.toml の [[kanji]] block に移行
"萩" = "はぎ"
"萱" = "かや"
"葡" = "ブ"
# 董 は core/kanji/overrides.toml の [[kanji]] block に移行
"葦" = "あし"
"葵" = "あおい"
# 葺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 蒐 は core/kanji/overrides.toml の [[kanji]] block に移行
# 蒔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 蒙 は core/kanji/overrides.toml の [[kanji]] block に移行
# 蒲 は core/kanji/overrides.toml の [[kanji]] block に移行
# 蒼 は core/kanji/overrides.toml の [[kanji]] block に移行
"蓉" = "ヨウ"
"蓑" = "みの"
"蓬" = "よもぎ"
"蓮" = "はす"
"蔓" = "つる"
"蔣" = "ショウ"
"蔦" = "つた"
"蔭" = "かげ"
# 蕃 は core/kanji/overrides.toml の [[kanji]] block に移行
"蕉" = "ショウ"
"蕎" = "キョウ"
"蕗" = "ふき"
"蕨" = "わらび"
"蕪" = "かぶら"
"蕾" = "つぼみ"
"薗" = "エン"
"薙" = "なぎ"
"薩" = "サツ"
"藁" = "わら"
# 蘇 は core/kanji/overrides.toml の [[kanji]] block に移行
"蘭" = "らん"
"蝦" = "えび"
"蝶" = "ちょう"
# 螺 は core/kanji/overrides.toml の [[kanji]] block に移行
"蟬" = "せみ"
"蟹" = "かに"
"蠟" = "ロウ"
"衿" = "えり"
"袈" = "ケ"
"袴" = "はかま"
"裟" = "サ"
# 裡 は core/kanji/overrides.toml の [[kanji]] block に移行
"裳" = "ショウ"
# 襖 は core/kanji/overrides.toml の [[kanji]] block に移行
# 訊 は core/kanji/overrides.toml の [[kanji]] block に移行
# 訣 は core/kanji/overrides.toml の [[kanji]] block に移行
"註" = "チュウ"
# 詢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 詫 は core/kanji/overrides.toml の [[kanji]] block に移行
# 誼 は core/kanji/overrides.toml の [[kanji]] block に移行
# 諄 は core/kanji/overrides.toml の [[kanji]] block に移行
# 諏 は core/kanji/overrides.toml の [[kanji]] block に移行
# 諒 は core/kanji/overrides.toml の [[kanji]] block に移行
# 諺 は core/kanji/overrides.toml の [[kanji]] block に移行
# 謂 は core/kanji/overrides.toml の [[kanji]] block に移行
# 讃 は core/kanji/overrides.toml の [[kanji]] block に移行
"豹" = "ひょう"
# 貰 は core/kanji/overrides.toml の [[kanji]] block に移行
# 賑 は core/kanji/overrides.toml の [[kanji]] block に移行
"赳" = "キュウ"
# 跨 は core/kanji/overrides.toml の [[kanji]] block に移行
"蹄" = "ひづめ"
# 蹟 は core/kanji/overrides.toml の [[kanji]] block に移行
# 輔 は core/kanji/overrides.toml の [[kanji]] block に移行
# 輯 は core/kanji/overrides.toml の [[kanji]] block に移行
"輿" = "こし"
# 轟 は core/kanji/overrides.toml の [[kanji]] block に移行
"辰" = "たつ"
"辻" = "つじ"
# 辿 は core/kanji/overrides.toml の [[kanji]] block に移行
"迂" = "ウ"
"迄" = "まで"
"迦" = "カ"
"迪" = "みち"
# 逗 は core/kanji/overrides.toml の [[kanji]] block に移行
# 這 は core/kanji/overrides.toml の [[kanji]] block に移行
# 逞 は core/kanji/overrides.toml の [[kanji]] block に移行
# 逢 は core/kanji/overrides.toml の [[kanji]] block に移行
# 遁 は core/kanji/overrides.toml の [[kanji]] block に移行
# 遥 は core/kanji/overrides.toml の [[kanji]] block に移行
# 遼 は core/kanji/overrides.toml の [[kanji]] block に移行
"邑" = "むら"
"郁" = "イク"
"鄭" = "テイ"
"酉" = "とり"
"醇" = "ジュン"
"醍" = "ダイ"
"醐" = "ゴ"
# 醬 は core/kanji/overrides.toml の [[kanji]] block に移行
# 釉 は core/kanji/overrides.toml の [[kanji]] block に移行
"釘" = "くぎ"
"釧" = "くしろ"
# 鋒 は core/kanji/overrides.toml の [[kanji]] block に移行
# 鋸 は core/kanji/overrides.toml の [[kanji]] block に移行
"錆" = "さび"
"錐" = "きり"
"錫" = "すず"
"鍬" = "くわ"
"鎧" = "よろい"
# 閃 は core/kanji/overrides.toml の [[kanji]] block に移行
"閏" = "うるう"
# 閤 は core/kanji/overrides.toml の [[kanji]] block に移行
# 阿 は core/kanji/overrides.toml の [[kanji]] block に移行
# 陀 は core/kanji/overrides.toml の [[kanji]] block に移行
"隈" = "くま"
# 隼 は core/kanji/overrides.toml の [[kanji]] block に移行
"雀" = "すずめ"
"雁" = "かり"
# 雛 は core/kanji/overrides.toml の [[kanji]] block に移行
"雫" = "しずく"
"霞" = "かすみ"
"靖" = "やす"
"鞄" = "かばん"
"鞍" = "くら"
"鞘" = "さや"
"鞠" = "まり"
# 鞭 は core/kanji/overrides.toml の [[kanji]] block に移行
# 頁 は core/kanji/overrides.toml の [[kanji]] block に移行
# 頌 は core/kanji/overrides.toml の [[kanji]] block に移行
# 頗 は core/kanji/overrides.toml の [[kanji]] block に移行
"頰" = "ほお"
# 顛 は core/kanji/overrides.toml の [[kanji]] block に移行
"颯" = "サツ"
# 饗 は core/kanji/overrides.toml の [[kanji]] block に移行
"馨" = "かおり"
# 馳 は core/kanji/overrides.toml の [[kanji]] block に移行
# 馴 は core/kanji/overrides.toml の [[kanji]] block に移行
# 駈 は core/kanji/overrides.toml の [[kanji]] block に移行
# 駕 は core/kanji/overrides.toml の [[kanji]] block に移行
"駿" = "シュン"
"驍" = "たけし"
# 魁 は core/kanji/overrides.toml の [[kanji]] block に移行
# 魯 は core/kanji/overrides.toml の [[kanji]] block に移行
"鮎" = "あゆ"
"鯉" = "こい"
"鯛" = "たい"
"鰯" = "いわし"
"鱒" = "ます"
"鱗" = "うろこ"
# 鳩 は core/kanji/overrides.toml の [[kanji]] block に移行
# 鳳 は core/kanji/overrides.toml の [[kanji]] block に移行
"鳶" = "とび"
"鴨" = "かも"
# 鴻 は core/kanji/overrides.toml の [[kanji]] block に移行
# 鵜 は core/kanji/overrides.toml の [[kanji]] block に移行
# 鵬 は core/kanji/overrides.toml の [[kanji]] block に移行
"鷗" = "かもめ"
"鷲" = "わし"
# 鷹 は core/kanji/overrides.toml の [[kanji]] block に移行
"鷺" = "さぎ"
"麒" = "キ"
"麟" = "リン"
"麿" = "まろ"
# 黎 は core/kanji/overrides.toml の [[kanji]] block に移行
# 黛 は core/kanji/overrides.toml の [[kanji]] block に移行
"鼎" = "かなえ"
# "龍" は core/kanji/overrides.toml の [[kanji]] block に移行
