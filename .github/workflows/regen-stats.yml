name: Regen STATS.md

# 辞書 (core/) / ルール (rules/) / regen_stats.py のいずれかが master に push されたら、
# STATS.md を再生成して diff があれば auto-commit する。
#
# bot 自身の commit は `[skip stats]` を含むので再 trigger されない。
# 既存 commit に手で `[skip stats]` を入れたい場合 (release commit 等) もスキップされる。

on:
  push:
    branches: [master]
    paths:
      - 'core/**/*.toml'
      - 'rules/**/*.toml'
      - 'tools/regen_stats.py'

permissions:
  contents: write

jobs:
  regen:
    name: STATS.md 自動再生成
    if: "!contains(github.event.head_commit.message, '[skip stats]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          # 後で `git push` するため fetch-depth=0 + token 必須
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: 再生成
        run: python3 tools/regen_stats.py

      - name: diff があれば commit + push
        run: |
          if git diff --quiet -- STATS.md; then
            echo "STATS.md is up-to-date, nothing to commit"
            exit 0
          fi
          echo "STATS.md has drift, committing..."
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add STATS.md
          git commit -m "chore: regen STATS.md [skip stats]"
          git push
